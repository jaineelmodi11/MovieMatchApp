# recsend_tests/users.yaml — create-or-get a test user (DB-only, no external keys)
url: http://localhost:3000/users/importOrGetId
method: POST
headers:
  Content-Type: "application/json"
body:
  firebaseUid: "test-uid-123"
  displayName: "Test User"
expected:
  status_code: 200
  body_contains:
    - userId
