# Used by docker-compose (and local dev). Copy to `.env` and fill in.
# `.env` is gitignored — never commit real values.

TMDB_API_KEY=your_tmdb_api_key_here
AI_SERVICE_KEY=any_shared_secret_between_api_and_ml
PG_PASSWORD=postgres
