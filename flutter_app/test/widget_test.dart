import 'package:flutter_test/flutter_test.dart';
import 'package:moviematch/main.dart';

void main() {
  test('MovieMatchApp can be constructed', () {
    expect(const MovieMatchApp(), isA<MovieMatchApp>());
  });
}
